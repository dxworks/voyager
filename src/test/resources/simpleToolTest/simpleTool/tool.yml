name: Simple Tool
resultsPath: ${toolHome}/simpleResult.txt
commands:
  win:
    - name: Pint to screen
      exec: echo "This is a test"
    - name: Write file each
      exec: echo This is the simple project result >> ${toolHome}/simpleResult.txt
    - name: Write file base
      exec: echo This is the simple base result >> ${toolHome}/simpleResult.txt
    - name: additional fields
      exec: echo memory - ${memory}, field - ${field}, default field - ${default}
  unix:
    - name: My command
      exec: echo This is the simple result > ${toolHome}/simpleResult.txt
defaults:
  default: default field value
